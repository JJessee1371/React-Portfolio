import React from 'react';

function ContactHeader() {
    return (
        <header className="header-text">Let's Connect!</header>
    );
};

function PortfolioHeader() {
    return (
        <header className="header-text">My Projects</header>
    );
};

export { ContactHeader, PortfolioHeader };